<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">

          <li class="nav-header">
            <div class="dropdown profile-element">
                  <span>
                    <% if get_current_user(current_user.id).avatar.present? %>
                        <img class="img-circle" src="<%=  get_current_user(current_user.id).avatar.url(:original) %>"  alt="" data-src="<%= get_current_user(current_user.id).avatar.url(:original) %>" data-src-retina="<%=  get_current_user(current_user.id).avatar.url(:medium) %>" width="50" height="50" />
                    <% else %>
                        <img alt="image" class="img-circle" src="<%= image_path('empty-state-avatar-1.png') %>"  width="50" height="50" style="background:white;"/>
                    <% end %>
                  </span>
                  <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                    <span class="clear">
                        <span class="block m-t-xs">
                            <strong class="font-bold"><%= current_user.first_name %> <%= current_user.last_name %></strong>
                        </span> <span class="text-muted text-xs block"><%= current_user.role.humanize.titlecase %> <b class="caret"></b></span>
                    </span>
                  </a>
              <ul class="dropdown-menu animated fadeInRight m-t-xs">
                <li><a href="<%= settings_user_management_profile_path(current_user.id) %>"><i class="fa fa-user fa-fw"></i> My Profile</a>
                </li>
                <li><a href="<%= settings_user_management_account_settings_path(current_user.id) %>"><i class="fa fa-cogs fa-fw"></i> Account Settings</a>
                </li>
                <li class="divider"></li>
                <li><a href="" class="btn-signout"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
                </li>
              </ul>
            </div>
            <div class="logo-element">
              IN+
            </div>
          </li>
            
            <li class="<%= is_active_controller('dashboard') %>">
                <%= link_to root_path do %>
                    <i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.metrics">Dashboard</span>
                <% end %>
            </li>
            
            <li class="<%= is_active_controller('patients') %>">
                <%= link_to patients_path do %>
                    <i class="fa fa-users"></i> <span class="nav-label" data-i18n="nav.metrics">Patients</span>
                <% end %>
            </li>
            
            <li class="<%= is_active_controller('appointments') %>">
                <%= link_to appointments_path do %>
                    <i class="fa fa-calendar"></i> <span class="nav-label" data-i18n="nav.metrics">Appointments</span>
                <% end %>
            </li>

            <li class="<%= is_active_controller('history') %>">
                <%= link_to history_index_path do %>
                    <i class="fa fa-history"></i> <span class="nav-label" data-i18n="nav.metrics">History</span>
                <% end %>
            </li>
            
            <% if super_and_admin_roles %>
                <li class="<%= is_active_path('settings') %>">
                    <a href=""><i class="fa fa-cogs"></i> <span class="nav-label" data-i18n="nav.dashboard">Settings</span> <span class="fa arrow"></span></a>
                    <ul class="nav nav-second-level collapse <%= is_active_controller('dashboards') %>">
                        <li><%= link_to "General", settings_general_index_path %></li>
                        <li><%= link_to "H  .  M  .  O   .", settings_health_maintenance_organizations_path %></li>
                        <li><%= link_to "User Management", settings_user_management_index_path %></li>
                    </ul>
                </li>
            <% end %>
            
        </ul>

    </div>
</nav>

